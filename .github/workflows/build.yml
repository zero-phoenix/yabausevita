name: Build YabauseVita (Phase 2)

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install clang 21
        run: curl https://apt.llvm.org/llvm.sh | sudo bash -s -- 21

      - name: Checkout vitasdk installer
        uses: actions/checkout@v4
        with:
          repository: vitasdk/vdpm
          path: vdpm

      - name: Checkout vitasdk samples (for debugScreen.c/.h)
        uses: actions/checkout@v4
        with:
          repository: vitasdk/samples
          path: external/vitasdk-samples

      - name: Install vitasdk (full toolchain)
        run: |
          cd vdpm
          ./bootstrap-vitasdk.sh
          ./install-all.sh
        env:
          VITASDK: /usr/local/vitasdk

      - name: Flush filesystem writes before compiling
        run: |
          sync
          sleep 5
          test -f /usr/local/vitasdk/arm-vita-eabi/include/psp2/kernel/processmgr.h && echo "OK: header present"

      - name: Configure
        run: |
          mkdir build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release ..
        env:
          VITASDK: /usr/local/vitasdk

      - name: Build
        run: cmake --build build --verbose
        env:
          VITASDK: /usr/local/vitasdk

      - name: Upload VPK
        uses: actions/upload-artifact@v4
        with:
          name: YabauseVita-phase2-vpk
          path: build/YabauseVita.vpk

      - name: Upload build log always (even on failure, for debugging)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-directory
          path: build/
